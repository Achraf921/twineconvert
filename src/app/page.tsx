/**
 * Stub root page — engine-only milestone.
 * UI / brand / actual landing page lives later. For now this is just a build
 * marker so the project compiles and deploys cleanly.
 */
export default function Page() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>conversion engine</h1>
      <p>scaffolded · UI deferred · tools forthcoming</p>
    </main>
  );
}
